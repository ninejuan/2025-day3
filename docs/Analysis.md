# Day3 로그 분석 및 대회 진행 가이드
---
본 파일은 2025년 6월 초 진행된 전국기능경기대회 연수 메모를 기반으로 Claude가 생성한 게시글입니다.

## 📋 주요 메모사항

### 🌐 NAT Gateway 확장성 이슈

**문제**: 만약 NAT Gateway의 최대 연결 개수(약 55,000개)를 넘어선다면 어떻게 해결해야 할까?

**해결방안**:

- **방법 1**: Subnet을 더 작은 단위로 쪼갠다. IP range가 있다면 이것도 좋은 방법
- **방법 2**: **NAT Gateway에 EIP를 여러개를 달면 된다. (이게 조금 더 좋은 방법)** ⭐

### 🔄 ALB 특성 및 배포 전략

#### **ALB Fail-Open 특성**

- **ALB는 기본적으로 fail-open** → unhealthy여도 200 응답이 올 수 있음 (health check 시간 중 요청 보낸 경우)

#### **바이너리 배포 전략**

- **V2 V3 binary 나가는 과제**에 대해서는 **배포를 얼마나 빨리 할 수 있는가**에 대해서 채점
- **바이너리 나눠주자 마자** 그 바이너리에 맞게 수정되어 request 시작됨 → **배포 나중에 하지 말고 빨리 할 수 있도록**
- **주의사항**: 그렇다고 바로 요청 끊어버리면, 과거에 들어간 요청에 대해서도 점수 잃게 됨 (특히 stress app)

#### **요청 패턴 모니터링**

- 요청 날릴 때, **user pattern, RED(Rate, Error, Duration) pattern**이 있음
- 요청 들어오다가 갑자기 안 들어오는 상황이 되면 의심해봐야 할 것
- 이상이 있다라고 감지할 수 있을 것

### 📊 성능 모니터링 및 문제 해결

#### **응답 시간 임계값**

- **정상 범위**: response time이 0.5초 이렇게 유지되다가
- **경고 상황**: 4초 이렇게 튀면 문제
- **한계값**: 서비스에서 5초 이내 응답해야 한다고 했는데, 6초 이렇게 나오면 문제 → **분석해야 함**

#### **병목 지점 진단 순서**

1. **ALB 레벨**: ALB라면 어떤게 문제인가 보고
2. **EC2 레벨**: 일반적으로 EC2 문제이기 때문에 리소스 사용량 봐야 함
3. **Database 레벨**: EC2 레벨 확인하고, DB 사용하고 있으면 DB 확인

**리소스 문제 예시**: 앱은 정상인데 오래 걸리는건 리소스 문제일 수 있음

- CPU 100%
- Memory 100%
- Network bandwidth 다 썼거나

#### **데이터베이스 부하 해결방안**

**전제조건**: 바이너리 수정할 순 없고, DB 부하가 생겼다면 어떻게 할까?

❌ **비추천**: RDS Prox

✅ **권장 해결책**:

**1. Scale-Up 전략**

- **첫번째는 scale-up**을 하면 됨
- r6i.large instance라면, 얘를 2대로 늘리는게 아닌 **서버 자체를 r6i.xlarge, 2xlarge 등으로 스케일 업**하면 됨

**2. 아키텍처 개선 (인스턴스 타입 제약 시)**

- 과제지에 r6i.large만 써야 한다면? 그러면 우리가 **아키텍처를 개선**할 수도 있음
- **ALB에서 GET/POST를 나눠줄 수 있음**:
  - GET → Read Replica 서버만
  - POST → Write Replica 서버만
- **주의사항**: POST가 read/write 모두 한다면 쉽진 않음. 이건 **앱 동작을 명확히 알고 나서 할 수 있는 시도**
- ⇒ **아키텍처 바꾼 후 시도해보고 판단할 수 있을 것**

**3. 캐싱 전략**

- 캐시를 한다면, **Redis를 만들고, 거기 하는게 좋음**

**핵심**: **중요한건 병목 파악**; **요청 어떻게 흘러가고 있는지**

## 💡 최적화 팁

### 💰 비용 절약 전략

- **비용을 적게 쓰고자 할 때, bastion을 안 만들고 SSM 쓰는 것도 방법일 것** ⭐
- **계속 모니터링 하면서 죽일 수 있으면 EC2 죽여서 비용 아끼기**

### 📈 모니터링 메트릭 상세 분석

#### **응답시간 측정 방식**

- **일반적**: request time을 보통은 **average**로 보여줌
- **문제점**: 1,000,000건 중 하나의 요청만 엄청 오래 걸리게 설계할 수도 있음
- **해결책**: 그런건 average 옵션이 아닌 **P99를 사용**; **가장 오래걸리는 1%만 볼 수 있음**

#### **요청 분석을 위한 로그 활용**

- **어떤 Request format(param, body)가 들어오는지 확인하기 위해서는 ALB 로그 확인하는게 매우 큰 도움이 될 것**

### 🚀 성능 향상 기법

#### **캐싱 전략**

- **CloudFront에서 GET 요청도 캐싱할 수 있긴 함**
- **캐싱할 수 있는 수단 매우 많음**

#### **네트워크 최적화**

- **일반적으로 DynamoDB는 HTTPS를 통해 다른 VPC에 있는 곳에 접근** (시간 오래 걸릴 수 있음)
- **그럴 때는 엔드포인트 같은거 써서 아마존 백본 통해 더 빠르게 할 수 있을 것**
- 그 외에 오래 걸릴만한게 얼마나 있나. 확인하면 좋음

⚠️ **중요한 주의사항**: **그러나 단순히 엔드포인트를 쓰면 빨라져 이렇게 공부하면 안됨.** **같은 리전에서는 되게 적은 숫자임.**

## ⚙️ 배포 및 운영 팁

### 🎯 헬스체크 및 트래픽 관리

#### **기본 원칙**

1. **인스턴스 Healthy할 때만, 트래픽 받는게 중요함**
   - 그냥 단순히 인스턴스를 ALB에 붙이는건 쉬울 것

#### **배포 시 고려사항**

2. **Readiness, Draining(min 30초), Graceful Shutdown을 잘 고려해야 함**
   - Container의 graceful shutdown 텀 좀 길게 주는 등의 조치 진행

### ⚖️ 배포 전략 vs 비용 최적화

#### **배포 방식별 특징**

- **Rolling 배포**: 비용 효율적
- **Blue/Green 배포**: 안정적이나 EC2 2배 비용

#### **Graceful Shutdown 시간 최적화**

3. **Graceful shutdown 텀이 길어지면 배포 오래걸리는 문제 발생함**
   - **3과제 채점할 때 보면 EC2 얼마나 쓰는지도 봄**
   - **Rolling을 안하고 Blue/Green을 하면, EC2 2대가 더 뜸**
   - **만약 배포를 조금 더 안정적으로 하기 위해 graceful shutdown 텀을 60초를 주면, 그만큼 비용 점수에서 깎일 것**
   - **최적화 방안**: 만약, 앱은 1초, 5초 안에 응답하는 앱이라면 → **graceful shutdown 텀 길게 잡을 필요 없음**

## 🔍 병목 분석 체크리스트

### 1. 요청 흐름 추적

- [ ] ALB → EC2 → Database 각 단계별 응답 시간 측정
- [ ] 네트워크 대역폭 사용량 확인
- [ ] CPU/메모리 사용률 모니터링

### 2. 데이터베이스 최적화

- [ ] RDS 연결 수 및 대기 시간 확인
- [ ] 쿼리 성능 분석
- [ ] Read/Write 분리 고려

### 3. 실시간 모니터링

- [ ] CloudWatch 대시보드 구성
- [ ] 알람 임계값 설정
- [ ] 로그 분석 자동화

## 🎯 대회 전략

### 📦 바이너리 배포 대응

1. **사전 준비**: 배포 스크립트 자동화
2. **모니터링**: 실시간 성능 지표 추적
3. **롤백**: 문제 발생 시 즉시 복구 계획

### 💵 비용 최적화 상세 전략

1. **리소스 모니터링**: 불필요한 인스턴스 식별
2. **Auto Scaling**: 트래픽 패턴에 따른 자동 조정
3. **예약 인스턴스**: 예측 가능한 워크로드 대상

### 🏆 채점 기준 대응

- **성능**: SLO 목표 달성 (응답시간, 처리량)
- **비용**: EC2 사용량 최적화 (3과제에서 중요)
- **안정성**: 가용성 및 장애 복구 능력

## 📝 추가 실무 노하우

### 성능 분석 심화 기법

- **P99 모니터링이 핵심**: 평균만으로는 이상치 탐지 불가
- **ALB 로그 파싱**: 특정 요청 패턴이 문제 원인인지 파악
- **단계별 병목 분석**: ALB → EC2 → DB 순서로 체계적 접근

### 비용 최적화 실전 팁

- **SSM vs Bastion**: 관리 편의성과 비용 절약 동시 만족
- **실시간 모니터링**: 불필요한 리소스 즉시 식별 및 제거
- **Graceful Shutdown 튜닝**: 앱 특성에 맞는 최적 시간 설정

### 아키텍처 개선 전략

- **GET/POST 분리**: DB 부하 분산의 핵심
- **캐싱 전략**: Redis 활용한 응답 시간 단축
- **VPC Endpoint**: 네트워크 최적화 (단, 동일 리전에서는 효과 미미)

## 🏅 3과제 채점 기준 및 전략

### ⭐ 절대 기준: 에러 제로 원칙

**잘 받는 기준**: **500 에러는 단 하나도 나오면 안됨** ⚠️

### 📊 서비스 레벨 모니터링: RED 메트릭

**RED 메트릭 상세 분석**:

- **Rate (요청률)**: Request 얼마나 잘 처리하고 있는지
- **Error (에러율)**:
  - **500 에러 없어야 하고**
  - **웬만하면 400도 없어야 함**
- **Duration (응답시간)**: ALB에서 걸리는 시간 확인

### 🎯 3과제 핵심 전략

#### **우선순위 1: 기본 구성 완료**

- **성능은 그 이후니까, 구성하는 것부터 연습하기**
- **DB에 데이터 넣는거 잘 검색하고 잘 연습하기**

#### **우선순위 2: 로그 분석 마스터**

**Athena 로그 활용법**:

- **어제 Athena 로그 알려줬는데, 이걸 적재적소에 써야한다고 했었음**
- **아테나 로그 본다고 다 해결 X** ⚠️

**로그 분석 단계별 접근**:

1. **어떤 타겟, API에 리스폰스 걸리는지 (ALB Level)** → **이건 아테나**
2. **서버 에러** → **서버 로그**
3. **가장 먼저 봐야할 것은 ALB Metrics** ⭐

#### **우선순위 3: 로그 해석 스킬**

**올바른 로그 분석 방법**:

- **ALB 로그는 틀린그림찾기 X** ⚠️
- **거기서 sent byte 다른거 보는게 아님**
  - **그 필드의 의미는 페이로드 크기임. 즉, 얼마든지 달라질 수 있음**
- **로그는 틀린그림찾기 X** → **로그 필드가 어떤 의미인지 알고 테스트해야 함**

### 🏆 3과제 고득점 전략

#### **시간 관리 최적화**

- **3과제는 곱게 고민할거 없음**
- **일단 로그, 메트릭 보면 어디 문제인지 다 나와있음**
- **프로비저닝 잘 되어있는지 확인하고 하면 1시간만 하고 가만히 있어도 점수 얻는게 3과제** ⭐

#### **실전 연습 방법**

**연습 환경 구성**:

- **3과제는 앱 개발해서 연습하고**
- **GPT한테 부하 정도는 잘 만들어주니까**
- **그라파나랩 K6 그거 있음. Node.js로 트래픽 부하 만들 수 있음**
- **그걸로 많이 연습해보면 좋음** 💪

## 🔧 실무 도구 및 기법

### 부하 테스트 도구

- **Grafana K6**: 트래픽 부하 생성
- **Node.js**: 부하 테스트 스크립트 작성
- **GPT**: 부하 테스트 시나리오 자동 생성

### 로그 분석 도구별 용도

- **ALB Metrics**: 1차 진단 (가장 먼저 확인)
- **Athena**: API별 응답시간 분석
- **서버 로그**: 애플리케이션 레벨 에러 분석
- **CloudWatch**: 실시간 모니터링 및 알람

### 핵심 성공 공식

```
1. 기본 구성 완료 (DB 연동, 프로비저닝)
2. ALB Metrics 우선 확인
3. 필요시 Athena로 세부 분석
4. 500 에러 절대 금지
5. 1시간 후 안정 상태 유지
= 3과제 고득점 ✅
```
